# SR-IOV Tests Flowchart - Complete Execution Flow with Checking Points

## 📊 Overall Test Suite Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    START SR-IOV TEST SUITE                          │
└────────────────────────┬────────────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │  ✓ CHECK 1: Cluster Connected      │
        │    - Can connect to API server?    │
        └────────┬───────────────────────────┘
                 │
         ┌───────┴────────┐
         │ YES (continue) │
         └────────┬───────┘
                  │
                  ▼
        ┌────────────────────────────────────┐
        │ ✓ CHECK 2: SR-IOV Operator Ready   │
        │   - Is operator pod running?       │
        │   - All 4 components running?      │
        │     * network-resources-injector   │
        │     * operator-webhook             │
        │     * sriov-network-config-daemon  │
        │     * sriov-network-operator       │
        └────────┬───────────────────────────┘
                 │
         ┌───────┴────────┐
         │ YES (continue) │
         └────────┬───────┘
                  │
                  ▼
     ┌──────────────────────────────────┐
     │ Load Test Data (9 Tests)          │
     │ - Test configurations            │
     │ - Device types to test           │
     └──────────┬───────────────────────┘
                │
                ▼
     ┌──────────────────────────────────────────────────────────┐
     │         LOOP: For Each Test Case (1-9)                  │
     │                                                          │
     │ Test configurations vary by test                         │
     └──────────┬───────────────────────────────────────────────┘
                │
                ▼
```

---

## 🔄 Individual Test Execution Flow (with Checking Points)

```
FOR EACH TEST (1-9):
│
├─────────────────────────────────────────────────────────────────────
│
├─ TEST 1: 25959 - Spoof Checking Enabled
│           Config: spoolchk="on", trust="off"
│
├─ TEST 2: 70820 - Spoof Checking Disabled  
│           Config: spoolchk="off", trust="on"
│
├─ TEST 3: 25960 - Trust Disabled
│           Config: spoolchk="off", trust="off"
│
├─ TEST 4: 70821 - Trust Enabled
│           Config: spoolchk="on", trust="on"
│
├─ TEST 5: 25963 - VLAN & Rate Limiting
│           Config: vlanId=100, qoS=2, minTxRate=40, maxTxRate=100
│
├─ TEST 6: 25961 - Auto Link State
│           Config: linkState="auto"
│
├─ TEST 7: 71006 - Enable Link State
│           Config: linkState="enable"
│
├─ TEST 8: 69646 - MTU Configuration
│           Config: mtu=1800, spoolchk="on", trust="on"
│
├─ TEST 9: 69582 - DPDK Functionality
│           Config: deviceType="vfio-pci" (or netdevice for Mellanox)
│
└─────────────────────────────────────────────────────────────────────

FOR EACH TEST:
    │
    ├─► FOR EACH DEVICE IN TEST DATA:
    │   │
    │   ├─► LOOP THROUGH DEVICES (6 types):
    │   │   • e810xxv   (Device ID: 159b)
    │   │   • e810c     (Device ID: 1593)
    │   │   • x710      (Device ID: 1572)
    │   │   • bcm57414  (Device ID: 16d7)
    │   │   • bcm57508  (Device ID: 1750)
    │   │   • e810back  (Device ID: 1591)
    │   │
    │   └─► (Some tests skip certain devices)
    │
    └─► [See detailed flow below]
```

---

## 📋 Detailed Per-Device Execution Flow with Checking Points

```
┌───────────────────────────────────────────────────────────────────┐
│           FOR EACH DEVICE IN TEST DATA                            │
└─────────────────┬─────────────────────────────────────────────────┘
                  │
                  ▼
         ┌────────────────────────────────┐
         │ STAGE 1: Device Check          │
         └────────┬───────────────────────┘
                  │
                  ▼
   ┌─────────────────────────────────────────┐
   │ ✓ CHECK 3: Device Exists on Workers    │
   │ - Is the device (PF) present?          │
   │ - Query: oc get sriovnetworknodestates│
   │ - Returns: device list with DeviceID  │
   └──────────┬────────────────────────────┘
              │
      ┌───────┴───────┐
      │               │
   YES│               │NO
      │         ┌─────▼─────┐
      │         │ SKIP Test │
      │         │ for Device│
      │         └───────────┘
      │
      ▼
   ┌─────────────────────────────────────────┐
   │ ✓ CHECK 4: Device Supports Config      │
   │ - For TEST 5: Skip if x710/bcm57414   │
   │ - For TEST 8: Check MTU support       │
   │ - For TEST 9: DPDK support check      │
   └──────────┬────────────────────────────┘
              │
      ┌───────┴───────┐
      │               │
   YES│               │NO
      │         ┌─────▼─────┐
      │         │ SKIP Test │
      │         └───────────┘
      │
      ▼
   ┌──────────────────────────────────────┐
   │ STAGE 2: Initialize Virtual Function │
   └────────┬─────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 5: Create VF Policy          │
   │ - Create SriovNetworkNodePolicy      │
   │ - numVfs: 2                          │
   │ - Resource name: <TestID>-<DevName> │
   │ - Query: oc apply -f policy.yaml    │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │FAIL
     │      ┌──────▼──────┐
     │      │ ABORT Test  │
     │      │ for Device  │
     │      └─────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 6: Wait for Policy Ready     │
   │ - Poll: sriovnetworknodestates      │
   │ - Expected status: "Succeeded"       │
   │ - Timeout: 30 minutes                │
   │ - Poll interval: 10 seconds          │
   │ - Command: oc get sriovnetworknod... │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │TIMEOUT
     │      ┌──────▼──────┐
     │      │ ABORT Test  │
     │      │ for Device  │
     │      └─────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ STAGE 3: Create Test Environment     │
   └────────┬─────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 7: Create Test Namespace     │
   │ - Namespace: e2e-<TestID>-<DevName> │
   │ - Command: oc create ns <namespace> │
   │ - Set as privileged namespace       │
   │ - Verify: namespace exists          │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │FAIL
     │      ┌──────▼──────┐
     │      │ CLEANUP &   │
     │      │ ABORT Test  │
     │      └─────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ STAGE 4: Create Network Configuration
   └────────┬─────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 8: Create SriovNetwork CR    │
   │ - Network Name: <TestID>-<DevName> │
   │ - Resource Name: <DevName>          │
   │ - Target Namespace: e2e-<TestID>   │
   │ - Config: Test-specific params      │
   │ - Command: oc apply -f network.yaml│
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │FAIL
     │      ┌──────▼──────────┐
     │      │ CLEANUP &       │
     │      │ ABORT Test      │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 9: Wait for NAD Creation     │
   │ - Monitor: NetworkAttachmentDef     │
   │ - Expected: NAD appears in namespace│
   │ - Timeout: 180 seconds (extended)  │
   │ - Poll interval: 3 seconds          │
   │ - Command: oc get net-attach-def   │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │TIMEOUT
     │      ┌──────▼──────────┐
     │      │ CLEANUP &       │
     │      │ ABORT Test      │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ STAGE 5: Create Test Pods            │
   └────────┬─────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 10: Create Client Pod        │
   │ - Pod Name: client                   │
   │ - Network: NAD (net1)                │
   │ - IP: 192.168.1.10/24                │
   │ - MAC: 20:04:0f:f1:88:01             │
   │ - Command: oc apply -f pod.yaml     │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │FAIL
     │      ┌──────▼──────────┐
     │      │ CLEANUP &       │
     │      │ ABORT Test      │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 11: Create Server Pod        │
   │ - Pod Name: server                   │
   │ - Network: NAD (net1)                │
   │ - IP: 192.168.1.11/24                │
   │ - MAC: 20:04:0f:f1:88:02             │
   │ - Command: oc apply -f pod.yaml     │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │FAIL
     │      ┌──────▼──────────┐
     │      │ CLEANUP &       │
     │      │ ABORT Test      │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 12: Wait for Pod Ready       │
   │ - Pod Phase: Running                 │
   │ - Readiness: Ready condition = True │
   │ - Timeout: 300 seconds               │
   │ - Command: oc get pod <pod-name>    │
   │ - Checks BOTH client & server       │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │TIMEOUT
     │      ┌──────▼──────────────┐
     │      │ Collect Diagnostics │
     │      │ CLEANUP & ABORT     │
     │      └─────────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ STAGE 6: Verify VF Configuration     │
   └────────┬─────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 13: Verify Interface Config  │
   │ - Get pod MAC from net1 interface   │
   │ - Command on pod: ip link show net1 │
   │ - Extract: MAC address              │
   │ - Verify: MAC matches assignment    │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │FAIL
     │      ┌──────▼──────────┐
     │      │ CLEANUP &       │
     │      │ ABORT Test      │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 14: Test-Specific Config     │
   │                                      │
   │ Depends on test type:                │
   │                                      │
   │ TEST 1/4 (Spoof/Trust):              │
   │ - Check: ip link show <nic>         │
   │ - Verify: "spoof checking on/off"  │
   │                                      │
   │ TEST 5 (VLAN & Rate):                │
   │ - Check: VF settings                │
   │ - Verify: vlan=100, qos=2           │
   │ - Verify: min_tx_rate=40Mbps        │
   │ - Verify: max_tx_rate=100Mbps       │
   │                                      │
   │ TEST 6/7 (Link State):               │
   │ - Check: Link state config          │
   │ - Verify: auto/enable setting       │
   │                                      │
   │ TEST 8 (MTU):                        │
   │ - Check on pod: ip addr show net1   │
   │ - Verify: mtu 1800                  │
   │                                      │
   │ TEST 9 (DPDK):                       │
   │ - Check: PCIDEVICE env variable    │
   │ - Run: testpmd application          │
   │ - Verify: "forwards packets"        │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │FAIL
     │      ┌──────▼──────────┐
     │      │ CLEANUP &       │
     │      │ ABORT Test      │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ STAGE 7: Verify Network Connectivity │
   └────────┬─────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 15: Check Link Status        │
   │ - Command: ip addr show net1        │
   │ - Skip if: NO-CARRIER (expected)    │
   │ - Example devices: x710, bcm57508   │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴────────┐
     │               │
   UP│               │NO-CARRIER
     │         ┌─────▼──────┐
     │         │ Skip Traffic│
     │         │ Tests      │
     │         └────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 16: IPv4 Connectivity Ping   │
   │ - Get server IPv4 from net1         │
   │ - Command: ip addr show dev net1    │
   │ - Extract: IPv4 address             │
   │ - From client: ping -c 3 <IPv4>    │
   │ - Verify: 3 packets received        │
   │ - Timeout: 30 seconds               │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  PASS│             │FAIL
     │      ┌──────▼──────────┐
     │      │ Log failure but  │
     │      │ don't fail test │
     │      │ (may not have   │
     │      │ IPv4)           │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 17: IPv6 Connectivity Ping   │
   │ - Get server IPv6 from net1         │
   │ - Extract: IPv6 address             │
   │ - From client: ping6 -c 3 <IPv6>   │
   │ - Verify: 3 packets received        │
   │ - Timeout: 30 seconds               │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  PASS│             │FAIL
     │      ┌──────▼──────────┐
     │      │ Log failure but  │
     │      │ don't fail test │
     │      │ (may not have   │
     │      │ IPv6)           │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 18: HTTP/Curl Traffic        │
   │ - Both directions: client ↔ server  │
   │ - Port: 8080 (implicit in tests)   │
   │ - Verify: Connection successful    │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  PASS│             │FAIL
     │      ┌──────▼──────────┐
     │      │ Test FAILED     │
     │      │ (critical)      │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ TEST PASSED ✓                      │
   │ - All checks passed                  │
   │ - Network verified working          │
   └────────┬─────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────┐
   │ STAGE 8: Cleanup (in defer)          │
   └────────┬─────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 19: Remove SriovNetwork CR   │
   │ - Command: oc delete sriovnetwork   │
   │ - Name: <TestID>-<DevName>          │
   │ - Namespace: openshift-sriov-network│
   │ - Expected: Network removed         │
   │ - Verify: NAD deleted in namespace │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │DELAYED
     │             │(finalizer)
     │      ┌──────▼──────────────┐
     │      │ Retry with timeout  │
     │      │ 180 sec (enhanced)  │
     │      └─────────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ ✓ CHECK 20: Delete Test Namespace    │
   │ - Command: oc delete ns <ns>        │
   │ - Timeout: 30 seconds               │
   │ - Verify: namespace removed         │
   └────────┬─────────────────────────────┘
            │
     ┌──────┴──────┐
     │             │
  YES│             │TIMEOUT
     │      ┌──────▼──────────┐
     │      │ Log but don't   │
     │      │ fail cleanup    │
     │      └─────────────────┘
     │
     ▼
   ┌──────────────────────────────────────┐
   │ NEXT DEVICE OR NEXT TEST             │
   └──────────────────────────────────────┘
```

---

## 📊 Checking Points Summary Table

| # | Checking Point | What It Verifies | Command/Method | Expected | Timeout |
|---|---|---|---|---|---|
| 3 | Device Exists | Physical device on worker | `oc get sriovnetworknodestates` | Device in list | N/A |
| 4 | Device Support | Device supports config | Device check | Not skipped | N/A |
| 5 | Create VF Policy | Policy created successfully | `oc apply -f` | Policy exists | 20s |
| 6 | Policy Ready | VF initialized on node | Poll syncStatus | "Succeeded" | 30m |
| 7 | Namespace Created | Test namespace exists | `oc create ns` | Namespace ready | 30s |
| 8 | SriovNetwork Created | Network resource created | `oc apply -f` | Network exists | 20s |
| 9 | NAD Created | Network attachment def generated | `oc get net-attach-def` | NAD exists | 180s |
| 10 | Client Pod Created | Pod resource created | `oc apply -f` | Pod exists | 20s |
| 11 | Server Pod Created | Pod resource created | `oc apply -f` | Pod exists | 20s |
| 12 | Pods Ready | Pods in Running + Ready | `oc get pod` | Phase=Running, Ready=True | 300s |
| 13 | Interface Config | Virtual interface configured | `ip link show net1` | MAC present | N/A |
| 14 | Test-Specific Config | Test parameters applied | Device-specific check | Config matches | N/A |
| 15 | Link Status | Interface has carrier signal | `ip addr show` | NOT NO-CARRIER | N/A |
| 16 | IPv4 Ping | IPv4 connectivity | `ping -c 3` | 3 packets received | 30s |
| 17 | IPv6 Ping | IPv6 connectivity | `ping6 -c 3` | 3 packets received | 30s |
| 18 | HTTP Traffic | Application layer connectivity | curl/HTTP request | 200 OK response | 30s |
| 19 | Network Removed | SriovNetwork CR deleted | `oc delete sriovnetwork` | Resource gone | 180s |
| 20 | Namespace Cleaned | Test namespace deleted | `oc delete ns` | Namespace gone | 30s |

---

## 🎯 Test Matrix

### Configuration Parameters by Test

```
╔═══════╦══════════════╦═══════════╦═══════════╦═══════════════════════╗
║ Test# ║ Test Name    ║ Spoofchk  ║ Trust     ║ Additional Config     ║
╠═══════╬══════════════╬═══════════╬═══════════╬═══════════════════════╣
║ 25959 ║ Spoof On     ║ on        ║ off       ║ (baseline)            ║
║ 70820 ║ Spoof Off    ║ off       ║ on        ║ (baseline)            ║
║ 25960 ║ Trust Off    ║ off       ║ off       ║ (baseline)            ║
║ 70821 ║ Trust On     ║ on        ║ on        ║ (baseline)            ║
║ 25963 ║ VLAN/Rate    ║ (none)    ║ (none)    ║ VLAN:100, QoS:2,      ║
║       ║              ║           ║           ║ MinTx:40, MaxTx:100   ║
║ 25961 ║ Link Auto    ║ (none)    ║ (none)    ║ linkState:auto        ║
║ 71006 ║ Link Enable  ║ (none)    ║ (none)    ║ linkState:enable      ║
║ 69646 ║ MTU Config   ║ on        ║ on        ║ mtu:1800              ║
║ 69582 ║ DPDK Mode    ║ (none)    ║ (none)    ║ deviceType:vfio-pci   ║
║       ║              ║           ║           ║ (or netdevice)        ║
╚═══════╩══════════════╩═══════════╩═══════════╩═══════════════════════╝
```

### Devices Tested (per test, unless skipped)

```
Test 1-4,6-8: All 6 devices
├─ e810xxv
├─ e810c
├─ x710 (NO-CARRIER, traffic skipped)
├─ bcm57414
├─ bcm57508 (NO-CARRIER, traffic skipped)
└─ e810back

Test 5: Skips: x710, bcm57414, bcm57508
├─ e810xxv
├─ e810c
└─ e810back

Test 9 (DPDK): Skips: bcm* (see line 484)
├─ e810xxv
├─ e810c
├─ x710
└─ e810back
```

---

## ✨ Key Checking Points Summary

1. **Stage 1 - Device Validation** (Checks 3-4)
   - Verifies physical hardware exists and supports the test

2. **Stage 2 - VF Initialization** (Checks 5-6)
   - Creates virtual functions and waits for node sync

3. **Stage 3 - Environment Setup** (Check 7)
   - Creates isolated test namespace

4. **Stage 4 - Network Config** (Checks 8-9)
   - Creates network resource and verifies NAD generation

5. **Stage 5 - Pod Creation** (Checks 10-12)
   - Creates test pods and verifies readiness

6. **Stage 6 - Configuration Verification** (Checks 13-14)
   - Verifies test-specific parameters applied

7. **Stage 7 - Connectivity Tests** (Checks 15-18)
   - Validates network connectivity at multiple levels

8. **Stage 8 - Cleanup** (Checks 19-20)
   - Removes resources and verifies cleanup

---

**Total Checking Points**: 18 per test  
**Total Tests**: 9  
**Total Devices**: 6 (with variations per test)  
**Total Possible Executions**: ~50+ device × test combinations

